# === Cloud deployment requirements ===
# MediaPipe, OpenCV, audio etc. are NOT needed on the server.
# Pose detection runs client-side in the browser via MediaPipe JS.
# The server only serves the Flask dashboard + practice page.

flask>=3.0.0
gunicorn>=21.2.0
edge-tts>=6.1.9
deep-translator>=1.11.0
thefuzz>=0.20.0
joblib>=1.3.0
scikit-learn>=1.3.0
numpy>=1.26.4

# Windows-only (local desktop mode)
mediapipe==0.10.9; sys_platform == "win32"
opencv-python==4.9.0.80; sys_platform == "win32"
Pillow>=10.3.0; sys_platform == "win32"
mss>=9.0.1; sys_platform == "win32"
pyttsx3>=2.90; sys_platform == "win32"
pywin32==306; sys_platform == "win32"
pyaudio>=0.2.14; sys_platform == "win32"
pygame>=2.5.0; sys_platform == "win32"
playsound>=1.3.0; sys_platform == "win32"
SpeechRecognition>=3.10.4; sys_platform == "win32"
